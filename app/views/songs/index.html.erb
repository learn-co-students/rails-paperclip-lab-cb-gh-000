<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> <%= image_tag song.album_cover.url(:thumb) %> <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
